export class Validator {

    /**
     * Evaluate if valid string.
     * @param {string} value 
     * @returns {boolean}
     */
    static email(value) {
        return true;
    }

    /**
     * Evaluate if valid password.
     * @param {string} value 
     * @returns {boolean}
     */
    static password(value) {
        return true;
    }

    /**
     * Evaluate if valid username.
     * @param {string} value 
     * @returns {boolean}
     */
    static username(value) {
        return true;
    }

}